# 기술 요약 (면접/설명용)

비개발자에게도 설명할 수 있도록 Q&A 형식으로 정리했습니다.

**1. 왜 웹앱 대신 Desktop App을 사용했는가?**
웹앱은 브라우저와 서버 사이에 데이터가 오가는 구조라, 미공개 재무정보를
다루기에는 "정보가 어딘가로 전송될 가능성"이 구조적으로 남습니다. Desktop
App은 내 PC 안에서만 실행되므로, 애초에 서버로 보낼 이유가 없는 구조를
만들 수 있습니다.

**2. 왜 PySide6를 사용했는가?**
Python으로 Windows 네이티브 데스크톱 UI를 만들 수 있는 대표적인 도구입니다.
무료(LGPL)로 상업적 이용이 가능하고, Windows 실행 파일(.exe)로 패키징하기
쉽습니다.

**3. 왜 Polars를 사용했는가?**
재무데이터 같은 표 형태 데이터를 빠르게 처리하기 위한 라이브러리입니다.
pandas보다 대용량 데이터에서 더 빠르고 메모리를 덜 씁니다. 특히 연도별
피벗, 증감률 계산 같은 작업에 적합합니다.

**4. PostgreSQL은 어디에 사용했는가?**
분석 결과(찾아낸 패턴, 과거 비교, 조사 질문)와 회계사의 리뷰 기록을
저장하는 데 씁니다. 로컬 PC에서만 돌아가며(127.0.0.1), 클라우드 DB는 코드
차원에서 거부됩니다.

**5. 미공개 재무정보를 어떻게 보호했는가?**
크게 세 가지입니다. (1) 네트워크 코드를 프로젝트 전체에서 한 폴더
(`app/public_data_collector/`)로만 격리했습니다. (2) 그 폴더 밖 어디서도
`requests`/`httpx` 같은 네트워크 라이브러리를 쓰면 자동 테스트가
실패하도록 만들었습니다. (3) 그 폴더 안에서도, 실제로 내보낼 수 있는
필드를 7개(회사명, 식별자, corp_code, 조회기간, 페이지)로 제한하는
Allowlist를 두고, 이 범위를 벗어나면 예외를 던져 요청 자체를 막습니다.

**6. 왜 외부 LLM API를 사용하지 않았는가?**
OpenAI/Claude/Gemini 같은 외부 LLM에 텍스트를 보내는 순간, 그 텍스트가
재무 패턴이든 조사 질문이든 회사 외부로 나가게 됩니다. 이 프로젝트는
"내부정보를 이용해 외부에 질문하지 않는다"가 핵심 원칙이라, 문서 검색·
관련도 판단에 필요한 AI는 전부 로컬에서 돌아가는 모델(sentence-transformers)
로 대체했습니다.

**7. Local AI란 무엇인가?**
사용자가 직접 준비해 로컬 폴더에 넣어둔 문장 임베딩 모델입니다.
인터넷 연결 없이 텍스트를 벡터로 변환할 수 있습니다. 모델이 없으면
관련 기능만 꺼지고, 나머지 핵심 기능(재무비율, 패턴 탐지 등)은 정상
작동합니다.

**8. Local RAG란 무엇인가?**
공개 문서(기사, 공시)를 조각(chunk)내고, Local AI로 벡터화한 뒤, 조사
질문과의 유사도로 관련 문서를 찾아오는 과정입니다(Retrieval-Augmented
Generation의 "검색" 부분만 로컬로 구현 — 별도의 생성형 LLM 답변 생성은
하지 않고, 관련 근거를 사람이 직접 검토하도록 보여주는 데 그칩니다).

**9. 뉴스 검색은 인터넷을 사용하는데 왜 내부 재무정보는 외부로 나가지
않는가?**
뉴스/DART API 요청에는 "회사명 + 조회기간"만 담깁니다. "재고가 38% 줄고
차입금이 70% 늘었는데 사업 확장인가?" 같은 내부 판단은 절대 검색어로
바뀌지 않습니다. 넓게 가져온 공개 문서와 내부 조사 질문을 비교하는 작업은
전부 PC 안에서, 문서가 이미 로컬로 들어온 다음에 이루어집니다.

**10. Public Data Collector는 어떤 정보만 외부로 보내는가?**
`PublicCollectionRequest`라는 데이터 구조가 받을 수 있는 필드 자체가
회사명/식별자/corp_code/조회기간/페이지 번호뿐입니다. 재무 데이터를 넣을
자리가 코드 구조상 존재하지 않습니다.

**11. 왜 "사업확장" 같은 내부 Investigation Question을 외부 검색어로 보내지
않는가?**
그 질문은 내부 재무 패턴에서 유도된 "가설"이기 때문입니다. 이걸 그대로
검색하면 회사의 미공개 상황을 외부 서비스(검색엔진, API 로그 등)에
노출시키는 것과 같습니다. 대신 공개된 회사명과 기간만으로 넓게 자료를
가져온 뒤, 그 자료가 질문과 관련 있는지는 로컬에서 판단합니다.

**12. 공개기사 중 관련기사를 어떻게 찾는가?**
조사 질문과 각 기사(조각)를 Local AI로 벡터화한 뒤, 코사인 유사도로
가장 가까운 것부터 순위를 매깁니다. 유사도가 일정 기준 이상이면
"POSSIBLE"(가능한 설명 후보)로 표시하고, 그 이상의 확정 표현("SUPPORTED",
"이것이 원인이다")은 자동으로 붙이지 않습니다 — 사람이 직접 확인해야
확정됩니다.

**13. Cross-Account Pattern은 어떻게 찾는가?**
계정을 "사업 의미 단위"(매출, 매출채권, 재고, CAPEX, 자금조달 등)로
묶고, 같은 묶음(cluster) 안에서 여러 계정이 동시에 notable하게 움직이면
하나의 Pattern으로 인식합니다. 모든 계정 쌍을 무작정 비교하지 않고, 미리
정의한 묶음(예: CAPEX/FINANCING) 안에서만 봅니다. 우선순위 점수는 "변동
크기 × 관련 계정 수"로 계산되는 실제 공식이며, AI가 임의로 매기는
점수가 아닙니다.

**14. Historical Pattern은 어떻게 찾는가?**
같은 계정의 과거 3~4개년 증감률과 올해 증감률을 비교합니다. 과거에도
같은 방향으로 notable하게 움직인 적이 있으면(RECURRING), 그보다 훨씬
커졌으면(INTENSIFIED), 과거에 없던 움직임이면(NEW_PATTERN), 방향이
뒤집혔으면(REVERSAL_PATTERN), 평소 수준이면(NORMAL_RANGE)으로
분류합니다. 전부 실제 계산식이며, AI가 "느낌"으로 분류하지 않습니다.

**15. 프로그램의 한계는 무엇인가?**
- 원인을 확정하지 않습니다 — "Review가 필요한 Pattern"까지만 알려주고,
  최종 판단은 회계사가 합니다.
- 실제 DART/뉴스 연동은 사용자가 API 키를 발급받아야 동작합니다.
- 정식 보안 감사를 받지 않은 개발/학습 단계의 코드입니다.
- PDF 재무제표 OCR은 아직 지원하지 않습니다(구조화된 Excel/CSV만 지원).
- Local AI 없이는 문서 관련도 검색 기능이 꺼집니다(핵심 재무분석 기능은
  영향받지 않습니다).
